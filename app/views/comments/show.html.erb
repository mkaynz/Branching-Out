<p>
  <tbody>
  <% @comments.each do |comment| %>
    <tr>
      <td><%= comment.body %></td>
    </tr>
  <% end %>
  </tbody>

 <p>
  <%= link_to "Create New Post", new_post_path, class: 'btn btn-primary' %>
</p>

</p>
