<head>
<style>
body {
  background-color: coral;
}
</style>
</head>

<h1>Create New Ad</h1>

<%= bootstrap_form_with model: @ad, url: choose_path, method: :post, local: true do |x| %>

    <%= x.text_field :title %>
    <%= x.submit %>

<% end %>

<%= link_to 'Cancel', plants_path, class: 'btn btn-primary' %>