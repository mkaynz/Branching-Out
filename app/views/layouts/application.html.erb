<!DOCTYPE html>
<html>
  <head>
    <title>
      <% if content_for?(:html_title) %>
        <%= yield(:html_title) %> |
      <% end %>
      Branching Out
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#"><h1>Branching Out 🌳</h1></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="true" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="navbar-collapse collapse show" id="navbarColor01" style="">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#"><%= link_to 'Home', home_path, class: 'nav-link' %>
          <span class="sr-only">(current)</span>
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'Post', posts_path, class: 'nav-link' %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'Shop', ads_path, class: 'nav-link' %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'Inbox', conversations_path, class: 'nav-link' %></a>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="text" placeholder="Search">
      <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
    </form>
  </div>
</nav>
  <body>
    <main role="main" class="container">
            <p class="mt-1 text-right">
              <head>
              <style>
              body {
                background-color: coral;
              }
              </style>
              </head>

            <% if user_signed_in? %>
              <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn btn-dark btn-dark'%>
            <% else %>
              <%= link_to 'Sign In', new_user_session_path, class: 'btn btn-dark btn-dark'%>
              <%= link_to 'Sign Up', new_user_registration_path, class: 'btn btn-dark btn-dark'%>
            <% end %>
            </p>
            <p>You have visited this site <%= pluralize @visit_count, 'time' %>.</p>
      <%= yield %>
    </main>
  </body>
</html>
